# Fog Compute Environment Variables
# Copy this file to .env and fill in secure values
# IMPORTANT: Never commit the actual .env file to version control

# Database Configuration
POSTGRES_USER=fog_user
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
POSTGRES_DB=fog_compute

# Security
SECRET_KEY=GENERATE_WITH_python_-c_import_secrets_print_secrets.token_urlsafe_32

# Grafana Admin Credentials
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Redis Configuration
REDIS_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
REDIS_URL=redis://:${REDIS_PASSWORD}@localhost:6379/0

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Generate a secure SECRET_KEY:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. Replace all CHANGE_ME_TO_SECURE_PASSWORD with strong passwords
# 4. Keep your .env file private and never commit it
